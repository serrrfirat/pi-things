# Optional: override GitHub username (defaults to `gh api user`)
# IRONCLAW_PR_DIGEST_USERNAME=your-github-username

# Optional: days without update before a PR is tagged "stale" (default: 2)
# IRONCLAW_PR_DIGEST_STALE_DAYS=2
